# Max and Min element in the array :-
print(arr)

# For max :-
print(max(arr))
print(apply(arr, 1, max))
print(apply(arr, 2, max))

# For min :-
print(min(arr))
print(apply(arr, 1, min))
print(apply(arr, 2, min))